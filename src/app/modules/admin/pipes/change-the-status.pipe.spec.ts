import { ChangeTheStatusPipe } from './change-the-status.pipe';

describe('ChangeTheStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeTheStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
